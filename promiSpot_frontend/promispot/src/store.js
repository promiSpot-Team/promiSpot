export const reducer = (state, action) => {
  // state의 초기값 설정
  if (state === undefined) {
    return {
      userId: 1
    }
  }
  var newState;
  switch (action.type) {
    case 'MAKE_MAP_MARKER': 
  }

  return newState
}

export default reducer;